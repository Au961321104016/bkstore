// src/types.ts (or wherever you want to define your types)
export interface CartItem {
    id: string;
    title: string;
    author: string;
    price: number;
    quantity: number; // ➕ Include quantity
  }
  